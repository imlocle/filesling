# Production dependencies
# Top-level packages only; pip-tools resolves transitive deps

paramiko>=3.5.1
pydantic>=2.0.0
pyside6>=6.10.0
send2trash>=1.8.3

# Optional: iOS device support (iPhone/iPad via USB)
# pymobiledevice3>=4.0.0
